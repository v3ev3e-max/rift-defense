# 검증 기록 — 2026-09-06

검증 환경: Windows, Node.js 24.18.0, TypeScript strict, Vite 7.3.6, Phaser 3.90.0.

| 검증 | 결과 |
| --- | --- |
| `npm install` | 성공, 취약점 0건 |
| `npm run build` | 성공, TypeScript 오류 없음, `dist/` 및 `sw.js` 생성 |
| `npm test` | **22개 통과** |
| `npm run test:e2e` | **12개 통과**, Chromium PC / Android 크기 / iPhone WebKit 각 4개 |
| `node scripts/verify-production.mjs` | 오프라인 재접속·전투 진입·소환·합성 성공, 개발 UI 미노출 |
| 주요 화면 브라우저 오류 | pageerror 0건 |

## 전투 완주

레벨 1 추천 덱(유리아·세라·루나·이브·리제), 시드 28, 추가 골드·무적 없이 소환/합성/연구/특성을 선택하는 시뮬레이션:

- 30웨이브 승리
- 게임 시간 **688.48초 (11분 28초)**
- 655 처치 / 최고 5성 / MVP 세라
- 획득 전투 GOLD 5,859 / 영구 CREDIT 1,842

이는 고정 전략·고정 시드에 대한 완주 확인이며 전체 플레이어의 승률 추정은 아닙니다.

## 브라우저에서 확인한 흐름

홈 → 추천 덱 저장 → 새로고침 복원 → 영구 레벨업 → 전투 진입 → 3회 소환 → 마우스 드래그 또는 터치 배치 → 3중 합성 → 직업 연구 → 웨이브 시작 → 적 처치 → 일시정지/재개 → 특성 3택 → 패배 결과 → 보상 저장 → 새로고침 유지.

별도 시나리오에서 개발 도구로 최종 보스에 바로 진입해 보스 등장·실제 공격에 의한 승리·승리 보상·저장 파일 내보내기/가져오기·전투 재입장 초기화를 확인했습니다.

첫 완주 검사는 순수 전투 시뮬레이션, 브라우저의 보스 검사는 개발 기능을 이용한 구간 테스트입니다. 둘을 구분합니다.

## 발견해 수정한 문제

- TypeScript DOM 컬렉션 iterable 설정 누락.
- 불필요한 CSS import가 Vite 빌드를 방해하던 문제.
- 특성 창에서 탭 전환/기기 회전 후 일시정지가 보이지 않던 문제. 선택 후 재개 창을 표시하며 회귀 테스트 추가.
- 드론 자폭 주기를 요원의 기본 사격 대신 실제 드론 사격 횟수로 집계.
- Vite preview의 `Vary: Origin` 응답 때문에 오프라인 모듈 캐시가 누락되던 문제. 동일 출처 정적 파일에 한해 `ignoreVary` 적용.

## 확인 범위

- 30/60/120Hz 및 x2의 게임 시간·처치·자원 일치 검증.
- 적 120개 풀 상한·재사용 검증.
- 모바일 주요 액션 버튼 높이 44px 이상 및 화면 경계 안 배치 검증.
- 실물 Android/iPhone에서 장시간 FPS·발열·브라우저 주소창 변화는 아직 측정하지 않았습니다.
- PWA 오프라인 검사는 Chromium에서 수행했습니다. iOS 설치 경험과 저장소 정리 정책은 실기기 확인 대상입니다.
- Phaser 엔진 청크는 약 1.2MB(압축 약 332KB)이며 전투 진입 시 지연 로딩합니다. Vite 크기 경고는 남아 있습니다.
- 외부 GitHub 저장소 업로드와 Vercel 실배포는 수행하지 않았습니다. 정적 배포 설정과 CI 파일은 포함했습니다.

스크린샷과 자동화 출력은 로컬 `artifacts/`에서 확인할 수 있으며 Git에서는 제외됩니다.
