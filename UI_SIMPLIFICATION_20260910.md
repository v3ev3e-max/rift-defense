# 전투 UI 간소화

- 전장 바로 아래에 `소환`, `속성 강화`, `DPS`, `전술` 네 가지 진입 버튼을 배치했다.
- 영웅 소환과 속성 강화 목록은 접힌 상태로 시작하며, 필요한 기능만 펼칠 수 있다.
- 배치된 요원을 선택하면 얼굴, 별, 역할, 킬, DPS, 사거리와 함께 `합성`, `판매`, 공격 우선순위를 바로 조작할 수 있다.
- 판매 가격은 실제 환급 골드로 표시하며, 투자 기록이 없는 테스트 요원은 `판매 +0G` 대신 `판매`로 표시한다.
- 중복된 하단 합성 버튼과 사용하지 않는 배치 확장 표시를 제거했다.
- PC, Android 세로 화면, iPhone WebKit에서 선택, 드래그 합성, 판매와 환급을 검증했다.

배포하지 않았으며 `local-test/index.html`로 실행한다.
